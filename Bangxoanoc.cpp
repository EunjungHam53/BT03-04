#include <bits/stdc++.h>
using namespace std;
#define F(i, a, b) for (int i = (a); i < (b); i++)

int main()
{
    int x, y, x_, y_;
    cin >> x >> y;
    x_ = x;
    y_ = y;
    int a[x][y];
    F(i, 0, x) F(j, 0, y) a[i][j] = 0;
    int dx = 0, dy = 0, s = 0, direct = H;
    while (x_ || y_)
    {

    }
}
